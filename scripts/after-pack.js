/**
 * electron-builder afterPack hook (optional post-pack steps).
 * @param {import('electron-builder').AfterPackContext} context
 */
module.exports = async function afterPack(context) {
  void context;
};
